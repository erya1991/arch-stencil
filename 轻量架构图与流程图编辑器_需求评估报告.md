# 轻量架构图与流程图编辑器需求评估报告

## 文档属性

- 输入材料：`C:\Users\65410\.codex\attachments\d3ee935e-51b5-4310-8228-7c67307fbc3d\pasted-text.txt`
- 输入材料类型：产品 MVP 需求说明书
- 评估范围：产品方向合理性、MVP 范围、实现复杂度、主要风险和开发量
- 当前工程状态：当前工作区未发现既有 `package.json`、`src` 前端源码或可复用编辑器实现，本次估算按新建前端工程口径估算
- 阶段边界：本报告只做需求评估，不进入开发规划、不修改业务代码

## 1. 总体结论

### 1.1 产品方向合理

“结构化文本 → 模板化成图 → 少量人工调整 → 高清导出”的主流程清晰，目标用户和使用场景明确，纯前端、本地文件、无账号和无云端协作的技术边界也适合 MVP。

模板与业务数据分离、模板与主题分离、编辑结果进入统一画布模型等要求是合理的，能够避免首版被某一个具体项目的业务内容绑死。

### 1.2 当前 MVP 定义偏大

需求虽然排除了账号、协作、数据库和在线模板市场，但仍同时包含：

- 结构化文本解析与可修正的树形预览；
- 4 类模板及其差异化布局；
- 可编辑节点、尺寸、样式和部分文字；
- 连线、连接点、自动跟随；
- 多选、框选、组合、对齐、等距、吸附、辅助线；
- 撤销重做和本地恢复；
- JSON 源文件、PNG、SVG、独立 HTML 多种输出。

这些能力叠加后，产品不再只是“自动生成图片”，而是一个小型的矢量画布编辑器。范围并非不可开发，但不建议把全部内容作为同一批次的 MVP 验收条件。

### 1.3 最需要先确认的取舍

建议先确认一期是否以“产品架构图可用闭环”为唯一首要目标。若一期同时要求 4 种模板、部分文字富文本、完整连线编辑、组合和独立 HTML 导出，开发量和联调验证量会明显上升，且容易出现功能都存在但编辑体验不稳定的问题。

## 2. 需求合理性评估

| 评估项 | 结论 | 说明 |
| --- | --- | --- |
| 产品定位 | 合理 | 聚焦结构化文本快速成图，和通用绘图工具形成差异化边界。 |
| 用户场景 | 合理 | 产品经理、方案人员、项目人员和研发人员的架构图、流程图场景明确。 |
| 纯前端 MVP | 可行 | 本地 JSON、浏览器本地恢复、静态部署和离线编辑均符合纯前端边界。 |
| 配置化模板 | 合理且必要 | 有利于新增模板时复用编辑器、导出和文件能力。 |
| 4 类模板同时进入 MVP | 偏激进 | 产品架构图和流程图的布局、节点语义、连线需求差异较大。 |
| 轻量人工编辑 | 需要收窄定义 | “拖动、改文字、改样式”较轻；组合、富文本、连接点、自动跟随、对齐等组合后已接近专业编辑器能力。 |
| 部分文字选择与富文本 | 高复杂度 | 需要稳定处理文字编辑态、节点选择态、快捷键、换行、导出和样式范围。 |
| 独立 HTML 导出 | 可行但有验证成本 | 需要保证导出后不依赖编辑器运行时、远程资源和选中态，同时保持文字和布局。 |
| 200 节点性能目标 | 可行但需要明确口径 | 取决于节点类型、富文本、连线数量、浏览器和导出尺寸，不能只按节点数量验收。 |

## 3. 需求中的关键复杂点

### 3.1 结构化文本解析并不只是识别缩进

材料同时要求支持 2 个空格、Tab、Markdown 列表符号、不规则缩进自动修正、最多 4 级、空行忽略、问题节点标记和预览中调整层级。需要先确定统一语法和纠错原则，否则同一份输入可能产生不同树结构。

尤其需要明确：

- 多个同级根节点是否允许；
- 5 级及以上内容如何处理；
- 混用 Tab 和空格时以什么规则判断层级；
- 不规则缩进是自动降级、自动补级，还是提示用户修正；
- 解析预览中的上移、下移、改层级是否会回写原始文本。

### 3.2 模板生成与手工编辑存在状态冲突

需求要求“更新并重新布局”时尽量保留位置、尺寸、颜色和文字样式，同时又要求新增、删除、层级调整和自动布局。要实现这一点，需要稳定识别“原节点”和“新节点”，并定义以下情况：

- 节点改名后是否仍视为同一节点；
- 同名节点如何区分；
- 父节点移动或改层级后子节点如何匹配；
- 用户手工删除的节点在再次更新文本时是否重新出现；
- 自动布局是否覆盖用户手工拖动的位置；
- 四级内容作为三级卡片内的列表项后，列表项是否可独立编辑、移动、连线和删除。

这是数据模型和交互规则的核心风险，不是单纯的 UI 细节。

### 3.3 “四级节点显示为列表”会放大编辑模型复杂度

需求一方面要求前端保存层级化 Node，另一方面规定产品架构图的四级节点通常不生成独立卡片，而是作为三级节点内部文本列表。需要确认四级内容是：

- 可独立编辑的结构节点；
- 仅作为父卡片中的富文本列表；
- 生成时可合并、但再次更新时仍可准确还原的隐藏结构。

如果这点不明确，解析树、画布节点、富文本和更新复用之间容易出现不一致。

### 3.4 画布编辑能力是主要开发量来源

节点拖拽和缩放本身不难，但以下能力需要共同处理选择态、事件冲突和历史记录：

- 节点与画布的缩放、平移；
- 框选、多选、组合；
- 文字编辑与快捷键隔离；
- 连接点拖拽和连线跟随；
- 对齐、等距、网格吸附、参考线；
- 分组移动、复制、删除和取消组合；
- 撤销重做覆盖上述所有操作。

这部分决定产品是否有“可用的编辑器体验”，也是比模板配置更大的工作量。

### 3.5 导出要求需要提前定口径

PNG 4x 在 3840×2160 或超宽画布下可能产生很大的浏览器 Canvas 内存压力；SVG 和独立 HTML 又要求真实文字、样式、圆角、连线和布局一致。需要提前确定：

- 超大画布的最大导出尺寸；
- 4x 导出是否允许浏览器因内存不足失败；
- 系统字体在不同机器上的替代规则；
- SVG 是否保留文字对象，字体缺失时如何降级；
- 导出“整个画布”和“选中区域”是否都需要透明背景；
- 独立 HTML 使用 SVG、HTML 节点还是两者混合。

### 3.6 性能目标还缺少测试口径

“200 个节点以内流畅”“自动布局 2 秒以内”可以作为方向性目标，但还缺少测试场景。至少要区分：普通节点数量、富文本字数、连线数量、组合层级、当前缩放比例和目标浏览器，否则验收时难以判断是否达标。

## 4. 开发量估算

以下估算基于：新建前端工程、1 名有经验的前端开发、已有常规 UI 组件能力、无后端、无账号体系；人日包含实现和基本自测，不包含正式视觉设计、长期兼容性维护和产品反复变更。实际采用成熟图形编辑库时，部分底层工作可减少，但仍需承担数据模型、模板、导出和交互适配成本。

| 范围 | 主要内容 | 预计开发量 |
| --- | --- | ---: |
| 技术验证 | 画布选中、拖拽、缩放、节点文字编辑、JSON 保存、PNG/SVG 导出可行性验证 | 3～5 人日 |
| 建议的一期闭环 | 产品架构图为主；结构化文本解析与预览；基础节点编辑；画布缩放/平移；主题基础能力；JSON 保存/打开；PNG 导出 | 20～30 人日 |
| 编辑体验增强 | 多选、框选、对齐、等距、网格吸附、辅助线、组合、格式刷、撤销重做、SVG 导出 | 12～20 人日 |
| 流程图能力 | 分层架构图、横向流程图、基础流程图、连接点、连线样式、连线跟随、自动布局、HTML 导出 | 18～30 人日 |
| 完整当前 MVP | 上述全部范围，加上部分文字富文本、更新图形的节点复用、异常解析、自动恢复和多浏览器验证 | 55～85 人日 |

因此，按当前需求全文实施：

- 单名前端开发，通常约需 12～18 周，仍需预留需求澄清和缺陷修复时间；
- 两名有经验的前端开发并行时，不能简单按人数减半，合理预期约 7～11 周；
- 若先做“产品架构图可用闭环”，可控制在约 4～6 周开发，再安排 1～2 周集中验证和修正。

以上不是承诺工期，而是用于判断范围级别的估算。当前工作区未发现既有工程，因此没有把复用现有组件、画布引擎或导出能力带来的节省计入。

## 5. 一期范围判断建议

### 材料明确且值得保留

- 结构化文本生成图形是核心价值；
- 产品架构图应作为第一优先级模板；
- 模板配置与业务内容分离；
- 生成后进入统一编辑模型；
- JSON 是编辑源文件，不能只保存最终 HTML；
- 本地运行、静态部署、无后端和无在线 API；
- PNG/SVG 输出应保持真实文字，而不是整图位图。

### 有材料依据但建议延后或降级

- 其余 3 种模板先做基础生成，不与产品架构图同时追求完整编辑体验；
- 部分文字选择、斜体、字体切换先降为节点级文字样式；
- 连接线先做基础连接和自动跟随，再扩展双向箭头、折线编辑等；
- 独立 HTML 导出可放到流程图能力之后；
- 主题先提供有限内置主题，不在首批追求复杂的“保护手工样式”规则。

### 不建议在确认前继续扩大的内容

- 自定义模板、模板导入导出；
- AI 自动生成业务内容；
- PPTX、Mermaid 等格式转换；
- 图标库、图片、泳道、复杂 UML/BPMN；
- 云端保存、协作和分享。

## 6. 进入开发规划前的待确认事项

1. 一期是按“产品架构图优先的可用闭环”推进，还是必须把需求中的 4 种模板全部纳入同一版验收？
2. 部分文字选择和富文本样式是否必须在第一版支持，还是先满足节点级文字样式？
3. 连线、连接点和自动跟随是否属于第一版核心验收，还是放到流程图阶段？
4. 独立 HTML 导出是否必须与 PNG、SVG 同批交付？
5. 期望的交付周期和开发人数是多少？这会决定是否需要进一步压缩首版范围。

## 7. 最终评估

**产品值得做，需求主线合理，纯前端实现可行；但当前版本的范围不适合直接作为“轻量 MVP”一次性开发。**

建议将“产品架构图生成与基础编辑”定义为第一交付目标，把完整流程图、连接线体系、富文本、组合与高级导出作为后续能力。这样更容易先验证核心价值，也能避免在编辑器底层能力尚未稳定前同时承担四套模板和多种导出格式的验收压力。

## 8. 第二版开发 Prompt 复评（2026-08-14）

### 8.1 相比上一版的有效收敛

本版已经明确产品不是通用流程图工具，并删除或后置了普通流程图、自由连线、锚点、复杂路径、解析树编辑器和复杂富文本等高风险内容。同时将模板数量收敛为 3 个，并把开发优先级拆为 P0 和 P1。

这些调整有效降低了范围。尤其是“重新生成优先完全重新生成”和“不提供自由新增连线”两条规则，避免了节点身份复用和连接关系维护成为首版阻塞项。

### 8.2 当前可行性判断

**本版可行，适合按纯前端项目实施。**

核心闭环已经可以落到：模板选择、文字模板复制、结构化文本解析、架构图生成、少量卡片编辑、JSON 源文件、PNG/SVG/HTML 导出。腾讯轻量服务器只需承载静态构建产物时，不需要后端服务，部署边界简单。

但它仍不是一个一两天即可完成的静态页面，原因是需要同时保证真实文本编辑、卡片拖动与缩放、区域层级、自动布局、主题、文件恢复和多格式导出的一致性。

### 8.3 按 Codex 实际执行能力的工作量

当前工作区没有既有前端工程，以下按从零搭建、由我负责实现、构建验证和交付说明估算：

| 阶段 | 交付内容 | 有效工作量 |
| --- | --- | ---: |
| 技术基线验证 | 工程启动、画布选型验证、真实文字编辑、拖动/缩放、PNG/SVG 导出可行性 | 1～2 个工作日 |
| P0 核心闭环 | 三栏编辑器、多层产品架构模板、文字模板/示例、解析、生成、基础文字和卡片编辑、拖动/尺寸、JSON、PNG | 4～7 个工作日 |
| P1 模板和排版 | 标准分层架构、业务域能力矩阵、自动布局、整理对齐、区域调整、3 套主题 | 3～5 个工作日 |
| P1 完善和交付 | SVG、独立 HTML、撤销/重做、异常输入、回归验证、生产构建、腾讯服务器静态部署说明 | 3～6 个工作日 |
| **合计** | **按当前 Prompt 完成一版可发布候选版本** | **11～20 个有效工作日** |

这里的“有效工作日”不包含等待用户确认、需求反复变更、服务器账号或 DNS/证书处理等待。若验收中要求多轮视觉微调、更多真实架构样例或兼容 Firefox/Safari，应额外预留 2～5 个工作日。

### 8.4 我会采用的开发编排

#### 阶段 0：先做可行性基线

先验证真实文字编辑、画布缩放与卡片拖动、节点尺寸调整、PNG/SVG 导出的共同实现路径。该阶段的目的不是做一个演示页面，而是避免后续发现所选图形实现方式无法同时满足编辑和导出。

#### 阶段 1：完成 P0 垂直闭环

先只把“多层产品架构图”打通：模板选择 → 复制文字模板 → 粘贴/解析 → 生成 → 修改 → 拖动/调尺寸 → 保存 JSON → 打开 JSON → PNG 导出。

阶段验收以真实的建筑产业工人平台、企业管理平台等不同内容替换测试，确认模板没有写死具体业务名称。

#### 阶段 2：抽取通用模板和数据边界

在第一套模板真实跑通后，再把解析结果、架构层、业务域、卡片、样式和主题整理成通用结构，补上标准分层架构和业务域能力矩阵。这样可以用真实问题验证模板抽象，而不是先写一个脱离实际的通用框架。

#### 阶段 3：补齐 P1 编辑体验

实现自动布局、整理对齐、区域整体调整、主题切换和基础撤销/重做，并逐项覆盖文字、颜色、拖动、尺寸等操作。自动布局与整理对齐要明确区别：前者允许完全重排，后者只做局部整理。

#### 阶段 4：完成输出和发布候选版

补齐 SVG 与独立 HTML 导出，验证导出结果不带编辑器界面、文字可读、样式和布局保持一致；随后执行构建、浏览器验收、静态服务器部署检查，并输出腾讯轻量服务器发布说明。

### 8.5 腾讯轻量服务器发布可行性

**可行。** 当前 Prompt 没有后端、数据库、登录或在线 API，适合构建为静态前端：

- 本地开发和验收使用开发服务器；
- 生产环境生成静态 `dist` 文件；
- 腾讯轻量服务器使用 Nginx 或同类静态 Web 服务托管；
- JSON 保存/打开和自动恢复继续在浏览器本地完成；
- 不需要开放后端接口端口。

服务器的操作系统、域名、HTTPS、Nginx 配置和上传方式尚未提供，因此本次只把静态部署产物和部署说明纳入开发范围；不能在未获得服务器访问授权前承诺已经完成线上发布。

### 8.6 仍需在开发前锁定的规则

1. “选中文字或对应文字区域”建议解释为**节点级文字样式**，否则会重新引入富文本编辑器复杂度。
2. 卡片拖动与架构区域拖动的关系需要固定：建议区域拖动带动内部元素，卡片拖动只改变卡片自身位置。
3. 自动扩展画布需要设置最大尺寸或超宽提示，避免 PNG 4x 导出因浏览器内存不足失败。
4. 模板示例中的 4 级内容如何映射到 `Card.items`，需要保持生成、编辑和 JSON 重开一致。
5. “打开 JSON”需明确只接受本工具版本生成的 JSON，并对损坏文件和版本不兼容给出提示。

### 8.7 第二版最终结论

本版 Prompt 已经达到可以进入研发规划的程度。我的建议是按 **P0 先交付可真实使用的多层产品架构图，再进入 P1 完善三模板、排版、主题和导出** 的顺序执行，预计 11～20 个有效工作日完成一版可发布候选版本。

确认以上范围和 5 项规则后，即可进入具体文件、组件、数据结构、实现顺序和验收用例规划。
