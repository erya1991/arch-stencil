# 架构图工坊开发交接文档

## 文档属性

- 当前状态：本地可运行候选版本
- 工程类型：React + TypeScript + Vite 纯前端单页应用
- 数据模式：浏览器本地编辑，`localStorage` 自动草稿，JSON 文件交换
- 后端/数据库：未建设

## 1. 项目概述

### 1.1 项目目标

将 AI 或人工整理的四级以内结构化文字快速转换为可编辑的专业架构图，允许少量视觉调整并导出正式文件。

### 1.2 当前实现范围

| 模块 | 当前实现 |
| --- | --- |
| 模板 | 3 个模板统一采用左侧层级标签 + 二至四级全量独立小卡片 |
| 输入 | 标题行、推荐 `L1:`～`L4:` 显式层级、兼容空格/Tab 缩进、列表符号去除、最多 4 级；支持复制 AI 提示词 |
| 画布 | 缩放、适应屏幕、Space 拖动画布、尺寸预设和自定义尺寸 |
| 编辑 | 层/业务域/卡片选择、双击文字、拖动、8 方向尺寸调整、删除 |
| 样式 | 节点级字体、字号、文字色、加粗、背景、边框、粗细、圆角；颜色支持选择器和标准颜色代码 |
| 排版 | 模板自动布局、整理对齐、区域整体移动和尺寸调整 |
| 文件 | `JSON` 编辑源文件下载和打开，浏览器自动草稿恢复，当前会话基础撤销/重做 |
| 导出 | PNG 2x/4x、SVG、独立 HTML |

### 1.3 未实现范围

局部富文本、自由连线、锚点、多选框选、格式刷、模板导入导出、图片/图标库、用户体系、云端保存、协作、AI 接口、流程图/BPMN/UML。

## 2. 技术架构

### 2.1 前端技术栈

- React 18.3
- TypeScript 5.6
- Vite 5.4
- 浏览器原生 File、Blob、Canvas 和下载能力

### 2.2 工程模块

- `src/App.tsx`：单页编辑器壳子、工具栏、模板输入、画布、属性面板、快捷键和状态。
- `src/types.ts`：文档模型、模板、主题、元素和解析结果类型。
- `src/data/templates.ts`：3 个模板的示例文字、文字模板、布局提示和 3 套主题。
- `src/lib/parser.ts`：标题识别、L1～L4 显式层级识别、缩进兼容、列表符号清理和异常提示。
- `src/lib/layout.ts`：3 套布局、主题应用、元素移动/调整尺寸和整理对齐。
- `src/lib/exporters.ts`：结构化文档转 SVG、独立 HTML、PNG 和 JSON 下载。
- `src/styles.css`：编辑器三栏布局、画布元素和属性面板样式。

### 2.3 文档模型

`ArchitectureDocument` 是保存和导出的唯一编辑源，包含 `templateId`、`themeId`、`sourceText`、`canvas`、`layers`、`groups` 和 `cards`。三个内置模板中二级、三级和四级内容都生成独立卡片，通过 `Card.level` 区分样式和布局；默认列宽约 196px。主题通过 `levelCardStyles` 预设各级卡片颜色。

## 3. 运行和构建

```powershell
npm.cmd install
npm.cmd run dev
npm.cmd run build
npm.cmd run preview
```

如果默认 npm 缓存目录权限不足：

```powershell
npm.cmd install --cache .npm-cache
```

生产文件在 `dist/`。当前构建验证命令为 `npm.cmd run build`，已通过 TypeScript 检查和 Vite 生产打包。

## 4. 部署说明

当前应用是静态文件，腾讯轻量服务器可以使用 Nginx 托管 `dist/`。不需要 Node 服务常驻、不需要数据库和后端端口。线上部署前需要根据服务器实际系统补充：

- 静态目录路径；
- Nginx 配置；
- 域名和 HTTPS 证书；
- 文件上传方式。

以上服务器事实当前尚未验证，不应视为已部署。

## 5. 已知问题与接手注意事项

- 文字编辑当前是单行标题编辑，节点级样式不支持同一节点内不同字号或颜色。
- 自动布局会重新生成全部图形，用户手工移动的位置不会保留。
- 主题切换会应用主题颜色，不保留手工样式。
- 自动草稿只保存在当前浏览器的 `localStorage`，清理浏览器站点数据、更换浏览器/设备后无法恢复；它不是服务器备份。
- Clipboard API 不可用时复制功能会降级使用隐藏文本框和浏览器 `execCommand('copy')`；若浏览器仍拒绝，则提示检查剪贴板权限。
- 三个模板当前将所有层级内容拆成独立卡片，卡片数量较多时会自动按层级列分布，尚未提供跨列拖拽后的智能避让。
- 颜色代码输入不在输入过程中强制校验，浏览器会按 CSS 规则渲染无效值；建议交付验收时使用标准 HEX/RGB/HSL 值。
- PNG 导出对极大画布会主动报错，避免浏览器 Canvas 内存异常；正式归档优先使用 SVG。
- 当前没有自动化单元测试和端到端测试；已完成生产构建和本地 HTTP 可访问性检查，需继续进行 Chrome/Edge 人工验收。

## 6. 交接检查清单

- [x] 工程可安装、可构建。
- [x] P0 多层产品架构图闭环可运行。
- [x] 3 个模板和 3 套主题已配置。
- [x] JSON、PNG、SVG、HTML 导出入口已实现。
- [x] `打开 JSON` 文案、L1～L4 输入提示、AI 提示词复制和自动草稿恢复已实现。
- [x] README 和 PROJECT_CONTEXT 已更新。
- [ ] 在目标腾讯服务器完成静态部署。
- [ ] 使用真实业务样例完成浏览器人工验收。

## 7. 版本修订记录

| 版本 | 日期 | 变更模块 | 变更说明 | 影响章节 |
| --- | --- | --- | --- | --- |
| 0.1.0 | 2026-08-14 | 首次工程实现 | 建立纯前端架构图生成与编辑候选版本 | 全文 |
| 0.1.1 | 2026-08-14 | 多层产品架构图模板 | 改为左侧层级标签和二至四级全量独立小卡片 | 1.2、2.3、5 |
| 0.1.2 | 2026-08-14 | 三模板布局与颜色 | 统一左侧标签/独立卡片、收窄默认宽度并增加颜色代码输入 | 1.2、2.3、5 |
| 0.1.3 | 2026-08-14 | 输入与本地草稿 | 增加 L1～L4 显式输入、AI 提示词、JSON 打开说明、浏览器自动草稿恢复，并统一工程名为 ArchStencil | 1.2、2.2、4、5 |
| 0.1.4 | 2026-08-14 | 剪贴板兼容 | AI 提示词和文字模板复制增加 Clipboard API 降级方案，适配普通 HTTP 或权限受限环境 | 1.2、2.2、5 |
