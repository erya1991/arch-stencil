# ArchStencil（架构图工坊）项目上下文

## 1. 已确认产品规则

- 产品核心是结构化层级文字快速生成架构图，不扩展为通用绘图工具。
- MVP 内置多层产品架构图、标准分层架构图、业务域能力矩阵 3 个模板。
- 模板必须配置驱动，业务名称必须来自用户输入，不得写死具体项目内容。
- 重新生成首版允许完全按模板重新布局，不保证复用手工位置和样式。
- 文字样式按节点级处理，不实现局部富文本编辑。
- 架构区域拖动带动内部元素；卡片拖动只改变卡片自身位置。
- 不提供自由新增连线、连接点、流程图节点、BPMN/UML、协作、登录、云端保存或 AI 接口。
- 源文字推荐使用 `L1:`～`L4:` 显式标记层级：L1 生成左侧层级标签，L2～L4 生成独立卡片；解析器兼容旧版空格/Tab 缩进格式。
- `打开 JSON` 仅接受当前结构可识别的本工具 JSON 编辑源文件；导出 PNG、SVG 和独立 HTML 不包含编辑器 UI。

## 2. 技术和运行边界

- 前端：React 18 + TypeScript + Vite 5。
- 运行方式：纯前端静态应用，支持本地开发和静态服务器部署。
- 数据：当前文档在 React 状态中维护；JSON 通过浏览器下载和文件选择打开；编辑变更会自动保存到当前浏览器 `localStorage`，没有服务端存储。
- 外部集成：无真实接口、无在线 API、无认证服务。
- 画布：DOM/CSS 元素负责编辑交互，导出模块从结构化文档独立生成 SVG/HTML/PNG。

## 3. 核心数据边界

- `ArchitectureDocument` 保存模板、主题、源文字、画布、层、业务域和卡片。
- `Layer` 在 3 个模板中都表示左侧层级标签；`Card` 表示可编辑内容卡片；新布局不再使用业务域大容器承载内部卡片。
- 三个模板的二级、三级和四级内容全部生成独立 `Card`，通过 `level` 区分视觉层级和默认颜色；默认列宽约 196px。多彩主题默认按 L1 层级绑定颜色族，业务域类 L1 通过 `paletteIndex` 再按业务域列拆分颜色，其他 L1 下的卡片保持同色系。
- `Card.items` 保留用于兼容旧 JSON 或后续需要卡片内明细的场景，当前三个内置模板优先使用独立卡片。
- 主题的 `levelCardStyles` 为通用回退配色；多彩主题通过 `cardPalette` 提供颜色族，通过模板的 `multiPaletteLayerIndexes` 指定允许按业务域拆色的 L1 层；同一 L1 或业务域列内的 L2/L3/L4 使用由深到浅的同色系；属性面板允许输入 HEX、RGB、RGBA、HSL 或其他 CSS 标准颜色值。
- `schemaVersion` 当前为 `1`；打开 JSON 时只接受当前结构能够识别的文件。
- 画布尺寸限制在前端表单约定范围内；PNG 导出对超大位图做浏览器内存保护。

## 4. 交互规则

- 双击层、业务域或卡片标题进入单行文字编辑。
- 选中元素后右侧修改节点级字体、字号、颜色、加粗、背景、边框和圆角。
- 卡片支持 Ctrl/Cmd 点击追加或取消选择，也支持鼠标框选；多选仅允许同一层级区域、同一 L 级别的卡片，右侧可批量修改文字和卡片样式。
- 左侧 L1 层级标签和内容卡片尺寸调整均保留 28px 可见性安全底线；默认布局按本层内容自适应高度，业务域容器仍保留结构性尺寸下限。
- 拖动空白画布或按住 Space 拖动移动视口；Ctrl/Cmd + 滚轮缩放。
- 自动布局完全按照当前模板和源文字重新生成；整理对齐只处理左侧 L1 标签右侧的独立卡片和旧版业务域容器卡片，保留 L1 标签的位置和尺寸，只调整右侧卡片的局部位置、间距和必要的卡片宽度，不重新生成文字或样式。
- 新生成的 `Card` 可保存 `layoutColumn`、`layoutGroupId`、`layoutOrder` 和 `layoutSlot` 作为整理对齐元数据；旧 JSON 缺少这些字段时，根据现有卡片位置推算列关系。
- Ctrl/Cmd + Z、Ctrl/Cmd + Y 和工具栏按钮控制基础历史记录，最多保留 50 个文档快照。
- 应用启动时如果发现未完成草稿，会先提示恢复或放弃；草稿不跨浏览器、设备或用户同步。
- 复制文字模板和 AI 提示词优先使用 Clipboard API；在普通 HTTP 或权限受限环境下自动降级为隐藏文本框复制，失败时给出权限提示。

## 5. 当前限制和延期项

- 尚未实现局部文字选择、复制粘贴节点、格式刷和自定义模板。
- 尚未实现自由连线、锚点、图层管理器和多选框选。
- 当前主题切换会将元素样式切换到主题颜色，不提供手工样式保护模式。
- 重新生成不保留已拖动位置；这是已确认的 MVP 简化规则。
- 正式浏览器兼容性、超大架构图性能和腾讯轻量服务器线上环境仍需验收。

## 6. 交付检查清单

- [x] 3 个模板可从内置配置选择，不需要导入模板。
- [x] 模板示例和用户源文字分离。
- [x] 生产构建通过。
- [x] JSON、PNG、SVG、HTML 的导出入口存在。
- [x] L1～L4 推荐输入格式、旧版缩进兼容和 JSON 源文件说明已接入。
- [x] 浏览器 `localStorage` 自动草稿保存与恢复已接入。
- [x] AI 提示词复制及 Clipboard API 降级复制已接入。
- [x] 同层级卡片 Ctrl/Cmd 多选、框选和批量样式编辑已接入。
- [x] 左侧 L1 层级标签和内容卡片高度已放宽为 28px 安全底线，默认布局支持按内容自适应。
- [x] README、项目上下文和开发交接文档已建立。
- [ ] Chrome/Edge 逐项人工验收。
- [ ] 腾讯服务器静态部署和域名 HTTPS 验证。
